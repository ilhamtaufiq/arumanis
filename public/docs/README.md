# Deprecated (Docsify)

User-facing docs are now **Fumadocs** built from `docs-site/` and served at `/docs` via `dist/docs`.

Do not edit these Docsify files for new content — update:

- `docs-site/content/docs/*.mdx`
- or source notes in `docs/user-guide/*.md` then re-port to MDX
