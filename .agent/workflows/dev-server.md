---
description: Start the development server for Arumanis frontend
---

# Start Dev Server

// turbo-all

1. Navigate to the project directory:
   ```bash
   cd c:\laragon\www\bun
   ```

2. Start the development server:
   ```bash
   bun run dev
   ```

3. The server will be available at `http://localhost:5173`

## Notes
- Make sure the backend API (apiamis) is running at `https://apiamis.test/api`
- Hot Module Replacement (HMR) is enabled by default
